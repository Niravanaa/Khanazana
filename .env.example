# Frontend
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_APP_URL=http://localhost:3000

# API
SUPABASE_SERVICE_ROLE_KEY=

# Database — local dev uses Docker (postgresql://postgres:postgres@localhost:5432/postgres)
# DATABASE_URL uses the PgBouncer pooler URL for app queries
# DIRECT_URL uses the direct connection URL for Prisma migrations
DATABASE_URL=
DIRECT_URL=
